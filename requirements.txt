# Core
numpy
pandas
pillow>=10.0.0
requests
tqdm
tqdm-joblib
joblib
pyyaml
python-dotenv
aiofiles
python-multipart

# API
fastapi
uvicorn[standard]

# Computer Vision / ML
opencv-python-headless>=4.9.0
torch>=2.1.2
torchvision>=0.16.2
ultralytics>=8.1.0
facenet-pytorch>=2.5.2
mediapipe>=0.10.9
transformers>=4.38.0
scikit-learn
scikit-image

# Utilities
TurboJPEG; platform_system!="Windows"
# On Windows, user might need to install libjpeg-turbo manually or use opencv fallback. 
# We keep PyTurboJPEG but it's optional in code (we saw the import, we should check if it's conditional).
# Actually fast_loader imports turbojpeg. Let's keep it but note it needs binary.
PyTurboJPEG

# CLIP
ftfy
regex
git+https://github.com/openai/CLIP.git
